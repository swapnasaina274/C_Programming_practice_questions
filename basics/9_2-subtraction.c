#include <stdio.h>

int main(){
    printf("Enter two numbers:");
    int a,b;
    scanf("%d %d",&a ,&b);
     int sub=a-b;
    printf("The subtraction of two numbers is:%d",sub);
    return 0;
}